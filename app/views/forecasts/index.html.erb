<%= content_for(:bars) do %>
  <%= render 'shared/tabs' %>
  <%= render 'shared/timeslottabs' %>
<% end %>

<% @forecasts.each do |forecast| %>
  <%= render 'shared/card', forecast: forecast %>
<% end %>

<script>
  document.querySelectorAll(`[data-day='${new Date().toJSON().slice(0,10)}'][data-time='11']`).forEach(function(obj) {
    obj.style.display="block";
  });




</script>
